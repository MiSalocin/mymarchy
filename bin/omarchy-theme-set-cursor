#!/bin/bash

# Call the VSCode theme setter with Cursor-specific parameters
myarchy-theme-set-vscode cursor "$HOME/.config/Cursor/User/settings.json" "$HOME/.local/state/myarchy/toggles/skip-cursor-theme-changes" Cursor
