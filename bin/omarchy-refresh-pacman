#!/bin/bash

# Take backup of existing files
sudo cp -f /etc/pacman.conf /etc/pacman.conf.bak
sudo cp -f /etc/pacman.d/mirrorlist /etc/pacman.d/mirrorlist.bak

sudo cp -f ~/.local/share/myarchy/default/pacman/pacman.conf /etc/pacman.conf

if [[ $1 == "edge" ]]; then
  sudo cp -f ~/.local/share/myarchy/default/pacman/mirrorlist-edge /etc/pacman.d/mirrorlist
  sudo sed -i 's|https://pkgs.myarchy.org/.*$arch|https://pkgs.myarchy.org/edge/$arch|' /etc/pacman.conf
  echo "Setting channel to edge"
else
  sudo cp -f ~/.local/share/myarchy/default/pacman/mirrorlist-stable /etc/pacman.d/mirrorlist
  sudo sed -i 's|https://pkgs.myarchy.org/.*$arch|https://pkgs.myarchy.org/stable/$arch|' /etc/pacman.conf
  echo "Setting channel to stable"
fi

echo

# Reset all package DBs and then update
sudo pacman -Syyu --noconfirm
