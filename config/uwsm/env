# Changes require a restart to take effect.

# Ensure Myarchy bins are in the path
export MYMARCHY_PATH=$HOME/.local/share/myarchy
export PATH=$MYMARCHY_PATH/bin:$PATH

# Set default terminal and editor
source ~/.config/uwsm/default

# Activate mise if present on the system
myarchy-cmd-present mise && eval "$(mise activate bash)"
